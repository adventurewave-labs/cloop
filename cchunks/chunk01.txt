<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>cloop — agentic loops for Claude Code</title>
<meta name="description" content="A zero-dependency Rust CLI that wraps claude --print in a configurable agentic loop: run, check, repeat until tests pass, a marker appears, or N iterations.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:ital,wght@0,400;0,500;0,600;0,700;1,400&family=IBM+Plex+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
:root{
  --ink:#0d1017; --panel:#151a23; --panel2:#10151d; --line:#232b37;
  --mist:#c9d3df; --dim:#78889a; --faint:#4c5a6a;
  --cyan:#56d4dd; --green:#54c98a; --amber:#e0a94f; --red:#f0645d; --violet:#b892e0;
  --kw:#7aa2f7; --ty:#56d4dd; --str:#a9c77d; --num:#e0a94f; --mac:#b892e0; --com:#5c6b7d; --chr:#c99a6a;
  --key:#8fb3ff; --flag:#7aa2f7;
  --mono:"IBM Plex Mono",ui-monospace,"SF Mono",Menlo,Consolas,monospace;
  --sans:"IBM Plex Sans",system-ui,-apple-system,"Segoe UI",Roboto,sans-serif;
}
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{margin:0;background:var(--ink);color:var(--mist);font-family:var(--sans);
  font-size:16px;line-height:1.6;-webkit-font-smoothing:antialiased;overflow-x:hidden}
a{color:var(--cyan);text-decoration:none}
a:hover{text-decoration:underline;text-underline-offset:3px}
.mono{font-family:var(--mono)}
.cyan{color:var(--cyan)}
em{color:var(--mist);font-style:normal;border-bottom:1px dotted var(--faint)}

/* cursor */
.cur{display:inline-block;width:.58em;height:1.02em;background:var(--cyan);
  margin-left:.14em;transform:translateY(.16em);animation:blink 1.05s steps(1) infinite}
@keyframes blink{50%{opacity:0}}

/* top bar */
.top{position:sticky;top:0;z-index:50;display:flex;align-items:center;justify-content:space-between;
  gap:20px;padding:14px clamp(18px,4vw,40px);background:rgba(13,16,23,.78);
  backdrop-filter:blur(10px);border-bottom:1px solid var(--line)}
.brand{font-family:var(--mono);font-weight:700;font-size:19px;letter-spacing:-.02em;color:#eef3f8}
.brand .cur{transform:translateY(.14em);width:.5em;height:.92em}
.nav{display:flex;gap:24px;font-family:var(--mono);font-size:13.5px}
.nav a{color:var(--dim)}
.nav a:hover{color:var(--mist);text-decoration:none}
@media(max-width:620px){.nav{display:none}}

/* layout */
.wrap{max-width:1120px;margin:0 auto;padding:0 clamp(18px,4vw,40px)}
section{padding:78px 0;border-top:1px solid transparent}
.sec{max-width:940px;margin:0 auto}
.reveal{opacity:0;transform:translateY(14px);transition:opacity .6s ease,transform .6s ease}
.reveal.in{opacity:1;transform:none}

/* hero */
.hero{padding:clamp(48px,7vw,92px) 0 64px}
.heroGrid{max-width:1120px;margin:0 auto;display:grid;grid-template-columns:1.04fr .96fr;
  gap:clamp(28px,4vw,56px);align-items:center}
.eyebrow{font-family:var(--mono);font-size:12.5px;letter-spacing:.16em;text-transform:uppercase;
  color:var(--cyan);margin:0 0 20px;display:flex;align-items:center;gap:10px}
.eyebrow::before{content:"";width:26px;height:1px;background:var(--cyan);opacity:.7}
h1{font-family:var(--mono);font-weight:700;font-size:clamp(34px,5.4vw,58px);line-height:1.02;
  letter-spacing:-.03em;margin:0 0 22px;color:#f2f6fa}
.lede{font-size:clamp(16px,1.7vw,19px);color:var(--mist);max-width:46ch;margin:0 0 30px}
.lede b{color:#eaf0f6;font-weight:600}
.cmdchip{display:flex;align-items:center;gap:12px;background:var(--panel);border:1px solid var(--line);
  border-radius:10px;padding:12px 12px 12px 16px;font-family:var(--mono);font-size:14px;max-width:440px}
.cmdchip .p{color:var(--green)}
.cmdchip code{color:var(--mist);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.meta{margin:22px 0 0;font-family:var(--mono);font-size:12.5px;color:var(--dim);letter-spacing:.02em}
.meta span{color:var(--faint);padding:0 8px}

/* terminal */
.term{background:linear-gradient(180deg,#12171f,#0f141b);border:1px solid var(--line);
  border-radius:12px;box-shadow:0 24px 60px -30px rgba(0,0,0,.8),0 0 0 1px rgba(86,212,221,.04);
  overflow:hidden}
.termBar{display:flex;align-items:center;justify-content:space-between;padding:11px 15px;
  border-bottom:1px solid var(--line);background:#0f141b}
.termBar .l{font-family:var(--mono);font-size:12px;color:var(--dim);letter-spacing:.02em}
.termBar .r{display:flex;align-items:center;gap:7px;font-family:var(--mono);font-size:11px;color:var(--dim)}
.pdot{width:7px;height:7px;border-radius:50%;background:var(--green);box-shadow:0 0 8px var(--green);
  animation:pulse 1.6s ease infinite}
@keyframes pulse{50%{opacity:.35}}
.termBody{padding:16px 18px;font-family:var(--mono);font-size:13px;line-height:1.62;
  min-height:430px;white-space:pre-wrap;word-break:break-word}
.termBody .ln{display:block}
.g-banner{color:var(--cyan)}
.g-step .gl{color:var(--violet)} .g-step{color:var(--mist)}
.g-step .tm{color:var(--faint);margin-left:8px}
.g-cmd{color:var(--dim);padding-left:18px}
.g-out{color:var(--mist);padding-left:18px}
.g-info .gl{color:var(--cyan)} .g-info{color:var(--mist)}
.g-warn .gl{color:var(--amber)} .g-warn{color:#d9c08a}
.g-ok .gl{color:var(--green)} .g-ok{color:var(--green);font-weight:600}
.g-ok.glow{text-shadow:0 0 18px rgba(84,201,138,.55)}
.tcur{display:inline-block;width:.5em;height:1em;background:var(--mist);transform:translateY(.14em);
  animation:blink 1.05s steps(1) infinite}
@media(max-width:860px){
  .heroGrid{grid-template-columns:1fr}
  .term{order:2}
  .termBody{min-height:0}
}

/* section headers */
.shead{display:flex;align-items:baseline;gap:14px;margin:0 0 26px}
.shead .gy{font-family:var(--mono);color:var(--cyan);font-size:20px;line-height:1}
.shead h2{font-family:var(--mono);font-weight:600;font-size:clamp(20px,2.6vw,27px);
  letter-spacing:-.02em;margin:0;color:#eaf0f6;white-space:nowrap}
.shead .rule{flex:1;height:1px;background:var(--line);transform:translateY(-6px)}
.lead{font-size:17px;color:var(--mist);max-width:66ch;margin:0 0 8px}
.prose{max-width:68ch}
.prose p{margin:0 0 16px}
.prose p:last-child{margin-bottom:0}

/* grids */
.two{display:grid;grid-template-columns:1fr 1fr;gap:clamp(20px,3vw,40px);align-items:start}
@media(max-width:820px){.two{grid-template-columns:1fr;gap:34px}}
.subh{font-family:var(--mono);font-size:12.5px;letter-spacing:.13em;text-transform:uppercase;
  color:var(--dim);margin:0 0 14px}

/* code blocks */
.block{position:relative;background:var(--panel);border:1px solid var(--line);border-radius:10px;
  overflow:hidden}
.block pre{margin:0;padding:16px 18px;font-family:var(--mono);font-size:13px;line-height:1.62;
  color:var(--mist);overflow-x:auto;white-space:pre}
.copyBtn{font-family:var(--mono);font-size:11px;color:var(--dim);background:rgba(255,255,255,.03);
  border:1px solid var(--line);border-radius:6px;padding:4px 9px;cursor:pointer;transition:.15s}
.copyBtn:hover{color:var(--mist);border-color:var(--faint)}
.copyBtn.done{color:var(--green);border-color:var(--green)}
.blockTop{position:absolute;top:9px;right:9px;z-index:2}

/* syntax colors */
.kw{color:var(--kw)} .ty{color:var(--ty)} .str{color:var(--str)} .num{color:var(--num)}
.mac{color:var(--mac)} .com{color:var(--com);font-style:italic} .chr{color:var(--chr)}
.key{color:var(--key)} .flag{color:var(--flag)}

/* callout */
.callout{display:flex;gap:14px;background:rgba(224,169,79,.06);border:1px solid rgba(224,169,79,.28);
  border-left:3px solid var(--amber);border-radius:9px;padding:18px 20px;max-width:820px}
.callout .ci{color:var(--amber);font-family:var(--mono);font-size:17px;line-height:1.4}
.callout p{margin:0;color:#e7dcc4}
.callout b{color:#f3ead3}
.callout code{font-family:var(--mono);color:#f3ead3}

/* tables */
.tbl{width:100%;border-collapse:collapse;font-size:14.5px}
.tbl td{padding:12px 14px;border-top:1px solid var(--line);vertical-align:top}
.tbl tr:first-child td{border-top:none}
.tbl td.mono{font-family:var(--mono);font-size:13px;white-space:nowrap}
.tbl td:last-child{color:var(--dim)}
.tblScroll{overflow-x:auto;border:1px solid var(--line);border-radius:10px;background:var(--panel)}
.tblScroll .tbl td{padding:11px 16px}

/* cards */
.cards{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
@media(max-width:780px){.cards{grid-template-columns:1fr}}
.card{background:var(--panel);border:1px solid var(--line);border-radius:11px;padding:22px 20px;
  transition:border-color .2s,transform .2s}
.card:hover{border-color:var(--faint);transform:translateY(-2px)}
.cardTag{font-size:12.5px;color:var(--dim);margin-bottom:14px}
.card h3{font-family:var(--mono);font-weight:600;font-size:16px;margin:0 0 9px;color:#eaf0f6;
  letter-spacing:-.01em}
.card p{margin:0;font-size:14px;color:var(--dim);line-height:1.62}

/* placeholders row */
.phs{display:flex;flex-wrap:wrap;gap:8px;margin-top:6px}
.ph{font-family:var(--mono);font-size:12.5px;color:var(--violet);background:rgba(184,146,224,.09);
  border:1px solid rgba(184,146,224,.24);border-radius:6px;padding:5px 9px}

/* source browser */
.src{background:var(--panel2);border:1px solid var(--line);border-radius:12px;overflow:hidden}
.srcTabs{display:flex;gap:2px;overflow-x:auto;padding:8px 8px 0;background:#0e131a;
  border-bottom:1px solid var(--line);scrollbar-width:thin}
.srcTab{display:flex;align-items:center;gap:8px;font-family:var(--mono);font-size:12.5px;color:var(--dim);
  background:transparent;border:none;border-bottom:2px solid transparent;padding:9px 13px;cursor:pointer;
  white-space:nowrap;transition:.15s}
.srcTab:hover{color:var(--mist)}
.srcTab.is-active{color:var(--cyan);border-bottom-color:var(--cyan)}
.srcTabLines{color:var(--faint);font-size:11px}
.srcTab.is-active .srcTabLines{color:var(--cyan);opacity:.7}
.srcPanel{display:none}
